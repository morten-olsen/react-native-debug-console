{
  "private": true,
  "workspaces": [
    "demo",
    "lib"
  ],
  "scripts": {
    "postinstall": "lerna bootstrap",
    "demo:ios": "lerna run --scope=react-native-debug-console-demo --stream ios"
  },
  "devDependencies": {
    "lerna": "^3.0.3"
  }
}
